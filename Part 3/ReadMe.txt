Follow Github link and open folder labeled "Part 3"
- select "MainProgram.zip" and then select "view raw", this will download the folder as "MainProgram.zip"
- once downloaded, select "extract all" and select destination to extract files to and make sure "show extracted files when complete" is selected
-then, open NetBeans and select "open project", open the downloaded folder, select the "MainProgram" project file and open
-Expand "MainProgram" project, expand "Source packages", then expand "com.mycompany.mainprogram" package and run program
-when prompted to select main class for execution, select "com.mycompany.mainprogram.MainProgramGUI" and your program will begin running!
